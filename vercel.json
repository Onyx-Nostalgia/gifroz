{
  "version": 2,
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "buildCommand": "python3 -m pip install uv && uv pip compile pyproject.toml -o requirements.txt",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ]
}